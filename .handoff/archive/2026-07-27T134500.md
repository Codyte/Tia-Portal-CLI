# Handoff · TIA Portal Openness API · 2026-07-27

## Goal
Testar a funcionalidade real da ferramenta: fluxo de engenharia ponta-a-ponta contra projeto TIA
real — clonar um acionamento existente (ex.: "mais uma bomba igual à BH-01A"), adaptar e inserir
na DB global, seguindo o padrão do próprio projeto.

## State
- HEAD: b874eec — working tree limpo. `rebuild.ps1` → ALL PASS, whitelist refeita.
- Done nesta sessão (2 bugs fechados, commitados, validados contra TIA real):
  - **`import-block` falso positivo no dry-run** → `Ops.RequireRootType` valida o root object
    antes de reportar `action` (`SW.Blocks.*` / `SW.Tags.PlcTagTable` / `SW.Types.*`).
    Dry sai 1 com `XML root object is 'SW.Tags.PlcTagTable', expected 'SW.Blocks.*'`.
    Teste offline `Ops.RequireRootType` + 4 combinações no AsBuilt (2 aceitas, 2 recusadas).
  - **`--device` só aceitava nome de estação** → `Hardware.FindDevice` cai pra busca recursiva
    nos `DeviceItems`; `--device "CPU CCO"` resolve `S7-1500/ET200MP station_1`. Not-found agora
    lista os 21 devices conhecidos.
  - Commit anterior levou junto os 5 arquivos pendentes (taskrun/rebuild/CLAUDE/PLANO/handoff).
- In progress: nada mid-flight.

## Decisions (and why)
- Validação de root no ponto compartilhado (logo após `RequireFile`), não por caller — 3 verbos
  cobertos com um helper; dry-run volta a valer como preflight.
- `--device` **resolve** o nome do CPU em vez de só sugerir na mensagem — o user copia o nome que
  `info`/`doctor` mostram; fazer funcionar é menor que ensinar a diferença.
- Achados vão pro `docs/PLANO.md`, sem doc separado de bugs.

## Next steps (ordered)
1. **Teste real do fluxo de engenharia.** Sequência: `prep-project.ps1 <Nome>` →
   `raio-x.ps1 <Nome>` → escolher equipamento-referência → `export-block` dos blocos da pasta
   modelo → dry de cada passo → aprovação do user → `--apply` → `compile --apply` +
   `save-project` → `diff-block` pra conferir. Alvo descartável (`SmokeTest_01`), não o AsBuilt.
2. **Lacuna conhecida: inserir membro novo na DB global.** O código *lê* a DB global e reescreve
   caminhos (`Replicate.cs:49`, `InstrumentFc.cs:87`); nenhum verbo *cria* instância de UDT.
   Caminho manual: `export-block "DB GLOBAL"` → inserir
   `<Member Name="BH-01B" Datatype="MotorDados">` na Section Static → `import-block --apply`.
   Perguntei ao user se quer o verbo `add-db-member --db X --name Y --type Z` — **resposta
   pendente**.
3. `--apply` ainda não exercitados: `import-ladder` (FlgNet escrito de memória, nunca validado
   contra TIA real) e hardware (`set-address`/`connect-subnet`/`add-device` — atributos Node e
   CreateIoSystem).
4. F1 propriamente dito (`docs/PLANO.md:171`).
5. Ressalvas de error-handling do code-review F3 (Standardize rebuild, FaultOb import sem
   try/catch por item) — não bloqueiam.

## Key files
- `src/Tia.Core/Replicate.cs` — `ReplicateFc.Run`: export → reescrita de XML (nome, número, path
  do DB global, tags IO, instance DB) → import. Motor do "clonar acionamento".
- `docs/examples/replicate-fc.json` — forma do config (BlocksFolder, EquipmentTypes, UdtNames,
  SourceNumbersToReplace, GlobalDb, StartNumber).
- `src/Tia.Core/Ops.cs:355-372` — `XmlRootType` / `RequireRootType`.
- `src/Tia.Core/Hardware.cs:15-33` — `FindDevice` + `HasItemNamed`.
- `scripts/taskrun.ps1` + `workspace/taskio/` — canal da sessão 1 (`cmd.json` entra,
  `out.txt`/`exit.txt` saem). Protocolo no `CLAUDE.md`, seção "Sessão 0 × sessão 1".
- `src/Tia.Tests/Program.cs` — testes offline; padrão pra adicionar novos (`Check` + registry).

## Open / blockers
- Alocação de número novo de equipamento e de `%M` livre: allocator existe só pra bits de alarme
  (`Profinet.cs:132`), não pra endereço de IO. Tag nova de IO no teste real = manual.
- Portal segue aberto na sessão 1 com o AsBuilt (compilado e salvo — autorizado). Mutações de
  smoke revertidas; 1011 blocos antes e depois.
- Openness single-session: nunca rodar `tia` em paralelo; um verbo por vez (`run --script` não
  isola steps — 1ª exceção aborta e descarta o já colhido).
- Classifier de permissões barra planos com `delete-*` mesmo em dry-run.
